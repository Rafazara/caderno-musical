import { RhythmSymbol } from './rhythm-symbol';import { RHYTHM_VALUES,type RhythmPattern } from '@/lib/music/rhythm';
export function RhythmGrid({pattern,active=-1}:{pattern:RhythmPattern;active?:number}){return <div role="img" aria-label={`${pattern.meter.numerator}/${pattern.meter.denominator}: ${pattern.label}`} className="grid grid-cols-8 border-y border-rule bg-paper-raised">{pattern.events.map((event,index)=><div key={index} style={{gridColumn:`span ${RHYTHM_VALUES[event.value].ticks}`}} className={`flex min-h-24 items-center justify-center border-x border-rule/50 ${active===index?'bg-brass-wash':''}`}><RhythmSymbol value={event.value} rest={event.rest} active={active===index}/></div>)}</div>}
